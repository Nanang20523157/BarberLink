<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center_horizontal"
    android:padding="16dp">

    <!-- Title -->
    <TextView
        android:id="@+id/tvTitle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        tools:text="JANUR BARBERSHOP"
        android:textSize="32sp"
        android:fontFamily="@font/bangers_regular"
        android:textColor="@color/black_font_color"
        android:gravity="center"
        android:layout_marginTop="35dp"
        android:layout_marginStart="-2dp" />

    <!-- Subtitle -->
    <TextView
        android:id="@+id/subtitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/template_string_p_o_s_barber"
        android:textSize="13sp"
        android:textStyle="bold"
        android:fontFamily="@font/georgia_regular"
        android:textColor="@color/black_font_color"
        android:gravity="center"
        android:layout_marginBottom="32dp" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:layout_weight="1"
        android:gravity="center">

        <!-- Checkmark Icon and Success Message -->
        <ImageView
            android:id="@+id/successIcon"
            android:layout_width="140dp"
            android:layout_height="140dp"
            android:layout_marginStart="-0.5dp"
            android:layout_marginTop="-120dp"
            android:src="@drawable/success"
            android:contentDescription="@string/image_success_icon"/>

        <TextView
            android:id="@+id/successMessage"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/Congratulations"
            android:layout_marginTop="-15dp"
            android:textSize="25sp"
            android:textStyle="bold"
            android:textColor="@color/black_font_color"
            android:gravity="center"
            android:layout_marginBottom="5dp" />

        <TextView
            android:id="@+id/successSubmessage"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/submessage_Congratulations"
            android:textSize="13sp"
            android:textColor="@color/black_font_color"
            android:gravity="center"
            android:layout_marginBottom="32dp" />

    </LinearLayout>

    <!-- Finish Button -->
    <com.google.android.material.button.MaterialButton
        android:id="@+id/btnDone"
        android:layout_width="match_parent"
        android:layout_height="50dp"
        android:layout_marginBottom="47dp"
        android:layout_marginHorizontal="40dp"
        android:backgroundTint="@color/black"
        android:text="@string/btn_done"
        android:textColor="#FFFFFF" />

</LinearLayout>
